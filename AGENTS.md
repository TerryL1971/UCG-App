# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

(Pinned to SDK 54, not "latest", because the published Expo Go app on the
App Store only supports SDK 54 as of 2026-08-23 — SDK 57 exists but Expo Go
hasn't caught up yet, which breaks device testing. Re-check this before
bumping the SDK again.)
