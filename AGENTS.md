# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

(Pinned to SDK 57, not "latest", so it stays intentional rather than
silently drifting. Bumped 2026-09-05 from SDK 54: the published Expo Go
app had moved on and no longer runs SDK 54 projects at all — Terry's
Expo Go reported "Client Version 57.0.9, supported SDK 57.0.0" and
refused to load this project until the bump. Re-check Expo Go's
currently-supported SDK before bumping again — it only ever runs one SDK
line, so this project either matches it or device testing breaks, in
either direction.)
